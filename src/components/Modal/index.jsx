import React from "react";

export default function Modal(){
    <div className='modal-container'>
        <button onClick={() => {closeModal(props.teste)}}>X</button>
        <h1>{props.titulo}</h1>
    </div>
}